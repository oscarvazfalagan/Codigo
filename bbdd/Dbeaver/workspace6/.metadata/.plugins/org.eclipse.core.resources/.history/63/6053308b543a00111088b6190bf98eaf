DELIMITER $$

DROP FUNCTION IF EXISTS NIF_cal$$

CREATE FUNCTION NIF_cal(caracteres VARCHAR(8))
	RETURNS VARCHAR(9)
	DETERMINISTIC
BEGIN
	DECLARE resto INT;
	DECLARE letra CHAR(1);
	IF SUBSTRING(caracteres,1,1)=x THEN SET resto = SUBSTRING(caracteres,2,7)%23;
	
	ALTER 
	
	
END$$
DElIMITER ;
