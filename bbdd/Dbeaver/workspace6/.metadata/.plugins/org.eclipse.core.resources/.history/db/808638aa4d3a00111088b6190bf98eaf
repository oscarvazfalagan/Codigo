DELIMITER $$

DROP FUNCTION IF EXISTS DNI_cal$$

CREATE FUNCTION DNI_cal(numeros CHAR(8))
	RETURNS CHAR(2)
	DETERMINISTIC
BEGIN
	DECLARE resto INT;
	SET resto = numeros%23;
	return resto;
END$$
DElIMITER ;

SELECT DNI_cal(54233516);