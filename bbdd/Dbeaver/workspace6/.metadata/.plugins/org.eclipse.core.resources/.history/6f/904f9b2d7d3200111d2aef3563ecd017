DELIMITER $$
CREATE DEFINER=`root`@`localhost` PROCEDURE `bd_transaciones`.`comprobarErro`()
BEGIN
	IF @@error_count > 0 OR ROW_COUNT() = 0 then 
		ROLLBACK;
	ELSE COMMIT;
	END IF;
END;
$$
DELIMITER ;








SET AUTOCOMMIT=0;
INSERT INTO tbl_ProbaTransaccions ( id, s, si) VALUES (9, "... comeza outra proba", NULL);
INSERT INTO tbl_ProbaTransaccions ( id, s, si) VALUES (10, "... superase o limite dbd_probatransaccions.comprobarErro tamaño de numero de carácteres da cadea, prodúcese un erro...", NULL); 
call comprobarErro();
SELECT * FROM  tbl_ProbaTransaccions;